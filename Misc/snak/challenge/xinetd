service challenge
{
    disable     = no
    type        = UNLISTED
    socket_type = stream
    protocol    = tcp
    port        = 1337
    wait        = no
    user        = snaak
    server      = /run.sh
}
